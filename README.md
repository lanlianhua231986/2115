```
   1 git branch -a
   2 git init 初始化项目
   3 git checkout -b dev  // 从master分支原封不动的克隆一份儿交给dev分支
   4 git status  // 查看当前状态
   5 git add .     //
   6 git commit -m '说明'
   7 git checkout master // 切换到master分支
   8 git merge dev  // 把dev分支的东西合并到master
```
